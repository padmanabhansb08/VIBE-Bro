# CODEBASE_RETROFIT.md Template

Used by vibe-init Stage 5B to generate vibe/CODEBASE.md for a legacy project.
This is the most critical file — it directly unblocks vibe-add-feature and vibe-fix-bug.
Every entry must correspond to an actual observed file or pattern.

---

```markdown
# CODEBASE — [Project Name]
> 📥 Generated by vibe-init on [date].
> Updated: [date] · [reason]
> All entries observed from actual codebase. Partially sampled sections are marked.

---

## 1. Tech stack

| Layer | Technology | Version |
|-------|-----------|---------|
| Runtime | [e.g. Node.js 20] | [from package.json engines or .nvmrc] |
| Framework | [e.g. Next.js 14.2] | [from package.json] |
| Language | [e.g. TypeScript 5.4] | [from package.json] |
| Database | [e.g. PostgreSQL 15] | [from docker-compose or env.example] |
| ORM | [e.g. Prisma 5.x] | [from package.json] |
| Auth | [e.g. NextAuth 4.x] | [from package.json] |
| Styling | [e.g. Tailwind 3.4] | [from package.json] |
| Testing | [e.g. Jest 29 / None] | [from package.json] |

---

## 2. Commands

```bash
# Development
[actual command — e.g. npm run dev]

# Build
[actual command — e.g. npm run build]

# Test
[actual command — e.g. npm test / not observed]

# Lint
[actual command — e.g. npm run lint / not observed]

# Database
[actual command — e.g. npx prisma migrate dev / not observed]
```

---

## 3. Folder map

[Every significant folder — one line per folder, purpose inferred from contents]

| Folder | Purpose |
|--------|---------|
| `[path/to/folder]/` | [inferred purpose] |
| `[path/to/folder]/` | [inferred purpose] |

> ⚠️ [If large codebase] Partially sampled — folders listed but not all files read.
> Update as new areas of the codebase are touched.

---

## 4. Key files

[Files that other files depend on — entry points, shared config, type definitions]

| File | Purpose | Depended on by |
|------|---------|---------------|
| `[path/file.ext]` | [purpose] | [who imports it] |
| `[path/file.ext]` | [purpose] | [who imports it] |

---

## 5. Routes / API endpoints

[Every route observed — grouped by resource or router file]

### [Resource name / Router file]
| Method | Path | Handler | Auth | Notes |
|--------|------|---------|------|-------|
| GET | `/[path]` | `[handler function]` | [yes/no/inferred] | |
| POST | `/[path]` | `[handler function]` | [yes/no/inferred] | |

[Repeat per resource / router]

> ⚠️ [If partially sampled] Routes from [N] router files documented.
> Additional routes may exist in unread files.

---

## 6. Data model

[Every schema / model observed — from Prisma schema, SQLAlchemy models, ActiveRecord, etc.]

### [Model/Table name]
| Field | Type | Constraints | Notes |
|-------|------|------------|-------|
| `[field]` | [type] | [PK/FK/required/unique] | |
| `[field]` | [type] | [PK/FK/required/unique] | |

**Relations:**
- [Model A] has many [Model B] via `[field]`
- [Model A] belongs to [Model B] via `[field]`

[Observed in: `prisma/schema.prisma` / `models/[name].py` / etc.]

[Repeat per model]

---

## 7. Key components

[Frontend only — significant components with their props and purpose]

### `[ComponentName]`
- **File:** `[path/to/Component.tsx]`
- **Purpose:** [what it does]
- **Props:** `[prop: type]`, `[prop: type]`
- **Used by:** [parent components or pages]
- **Notes:** [anything notable about its implementation]

[Repeat for significant components]

> ⚠️ [If partially sampled] [N] components documented from [N] total.
> Document additional components as they are touched.

---

## 8. Services / modules

[Business logic modules, external service wrappers, shared utilities]

### `[ServiceName / module name]`
- **File:** `[path/to/service.ts]`
- **Purpose:** [what it does]
- **Exports:** `[function/class names]`
- **Dependencies:** [what it imports from]
- **Notes:** [anything notable]

[Repeat per service / module]

---

## 9. Integration points

[External APIs, third-party services, auth providers — anything outside the codebase]

| Service | Purpose | SDK/Library | Config location |
|---------|---------|------------|----------------|
| [e.g. Stripe] | [payments] | [stripe npm] | [env var: STRIPE_KEY] |
| [e.g. SendGrid] | [email] | [sendgrid npm] | [env var: SENDGRID_KEY] |
| [e.g. Cloudinary] | [image storage] | [cloudinary npm] | [env var: CLOUDINARY_URL] |

> Environment variables required: [list from .env.example]
> Never hardcode these values — always read from environment.

---

## 10. Patterns

[Confirmed patterns from Stage 3 sampling — with specific file examples]

### Import pattern
[absolute / relative / barrel exports]
```[language]
// Example from [actual file]:
[actual import line]
```

### Component pattern
```[language]
// Example from [actual file]:
[actual component structure — 10-15 lines]
```

### API call pattern
```[language]
// Example from [actual file]:
[actual API call — 10-15 lines]
```

### Error handling pattern
```[language]
// Example from [actual file]:
[actual error handling — 10-15 lines]
```

### Test pattern
```[language]
// Example from [actual file — if tests exist]:
[actual test structure — 10-15 lines]
```
[If no tests: "No test patterns observed — no test suite found during init."]

---

## Changelog

| Date | Change |
|------|--------|
| [today] | Generated by vibe-init — [N] files read |
```
