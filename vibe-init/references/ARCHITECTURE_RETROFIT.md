# ARCHITECTURE_RETROFIT.md Template

Used by vibe-init Stage 5A to generate vibe/ARCHITECTURE.md for a legacy project.
Every placeholder must be filled from observed code — never invented.

---

```markdown
# ARCHITECTURE — [Project Name]
> 📥 Generated by vibe-init on [date] from codebase analysis.
> Reflects patterns observed in the actual code — not planned decisions.
> Update this file as new patterns are deliberately introduced.
> Every agent session reads this file before writing code.

---

## Project type
[Web app / Mobile app / API service / CLI tool / Full-stack / Other]
[Framework: e.g. Next.js 14 / FastAPI / Rails 7 / Flutter 3.x]

---

## Architecture overview

[ASCII diagram of the actual layers as observed — e.g.]

```
┌─────────────────────────────────────┐
│           Client (Next.js)          │
│  pages/ · components/ · hooks/      │
└─────────────────┬───────────────────┘
                  │ HTTP / API routes
┌─────────────────▼───────────────────┐
│          API Layer (Next.js API)    │
│  pages/api/ · middleware/           │
└─────────────────┬───────────────────┘
                  │ Prisma ORM
┌─────────────────▼───────────────────┐
│         Database (PostgreSQL)       │
│  prisma/schema.prisma               │
└─────────────────────────────────────┘
```

[Observed in: entry points, package.json, folder structure]

---

## Folder structure

[Exact observed structure with one-line purpose per folder]

```
[project-root]/
├── [folder]/          ← [purpose — observed]
│   ├── [sub-folder]/  ← [purpose — observed]
│   └── [sub-folder]/  ← [purpose — observed]
├── [folder]/          ← [purpose — observed]
└── [folder]/          ← [purpose — observed]
```

**Folder rules inferred from observed patterns:**
- [Rule 1] [Observed in: example file]
- [Rule 2] [Observed in: example file]
- [Rule N] [Observed in: example file]

---

## Naming conventions

[All conventions observed — specific, with examples from actual files]

- **Files:** [camelCase / kebab-case / PascalCase] — e.g. `[actual example]`
- **Components:** [PascalCase] — e.g. `[actual example]`
- **Functions:** [camelCase / snake_case] — e.g. `[actual example]`
- **Variables:** [camelCase / snake_case] — e.g. `[actual example]`
- **Constants:** [UPPER_SNAKE / camelCase] — e.g. `[actual example]`
- **API routes:** [/kebab-case / /camelCase] — e.g. `[actual example]`
- **Database tables:** [snake_case / PascalCase] — e.g. `[actual example]`
- **CSS classes:** [BEM / utility / camelCase] — e.g. `[actual example]`

[Observed in: list of files sampled]

---

## State management

**Approach:** [Local state only / Context API / Zustand / Redux / Pinia / MobX / none observed]

**Rules inferred from observed patterns:**
- [Rule 1] [Observed in: example]
- [Rule 2] [Observed in: example]

[If none observed: "State management approach not determined during init.
Observe and document as the codebase is worked on."]

---

## Error handling

**Approach:** [Try/catch + error boundary / Global handler / None observed]

**Pattern observed:**
```[language]
[Actual error handling code snippet from a sampled file — 5-10 lines max]
```
[Observed in: filename]

**Rules:**
- [Rule inferred from pattern]
- [Rule inferred from pattern]

[If inconsistent: "Error handling is inconsistent across the codebase.
Standardise pattern as files are touched."]

---

## Backend architecture

[Skip entire section if frontend-only project]

**API style:** [REST / tRPC / GraphQL] [Observed in: route files]

**Business logic:** [Route handlers / Service layer / Controller/Service/Repository]
[Observed in: folder structure + sampled files]

**Input validation:** [Zod / Joi / Pydantic / None observed]
[Observed in: route files]

**Response format:** [Envelope observed: e.g. `{ data, error }` / No consistent format]
[Observed in: route files]

---

## Testing

**Test runner:** [Jest / Vitest / pytest / None found]
**Test location:** [Co-located `*.test.*` / `__tests__/` / `tests/` / Not found]
**Coverage:** [Present — [%] / Present — unknown % / Not configured]

**Commands:**
```bash
[actual test command from package.json or Makefile]
```

**Coverage observed:**
- Unit tests: [present / absent]
- Integration tests: [present / absent]
- E2E tests: [present / absent]

[If no tests: "No test suite found during init. Adding tests is recommended
before running vibe-review — findings will include this as P1."]

---

## Code quality

**Linter:** [ESLint / Ruff / Flake8 / None found]
**Formatter:** [Prettier / Black / None found]
**TypeScript:** [Strict / Lenient — any usage observed / Not used]

**Observed lint config:**
[Key rules from .eslintrc / pyproject.toml — list the 3-5 most notable]

**Commands:**
```bash
[actual lint command]
[actual format command if present]
```

---

## Dependencies

**Package manager:** [npm / yarn / pnpm / pip / poetry]
**Lock file:** [present / absent]
**Audit:** [Run `npm audit` / `pip-audit` before committing changes]

**Key dependencies (from manifest):**
| Package | Version | Purpose |
|---------|---------|---------|
| [name] | [version] | [purpose] |

---

## Tech stack

| Layer | Technology | Version | Notes |
|-------|-----------|---------|-------|
| Runtime | [Node.js / Python / Go / etc.] | [version] | |
| Framework | [Next.js / FastAPI / Rails / etc.] | [version] | |
| Database | [PostgreSQL / MySQL / MongoDB / etc.] | [version] | |
| ORM | [Prisma / SQLAlchemy / ActiveRecord / etc.] | [version] | |
| Auth | [NextAuth / Passport / JWT / etc.] | [version] | |
| Styling | [Tailwind / CSS Modules / styled-components] | [version] | |
| Deployment | [Vercel / Railway / AWS / Docker] | — | [inferred from config] |

---

## Known gaps from init

[Honest list of what couldn't be determined from code alone]

- [ ] [Gap 1] — [how to fill it]
- [ ] [Gap 2] — [how to fill it]

[Standard gaps to always check:]
- [ ] Deployment environment and CI/CD pipeline — check with team
- [ ] Environment variables and their purposes — check .env.example + team
- [ ] External service credentials and rate limits — check with team
- [ ] Any feature flags or A/B testing setup — check with team
```
