# README_TEMPLATE.md

Used by vibe-document Step 4.
Fill every section from actual project files — never from memory or assumption.

---

```markdown
# [Project Name]

[One sentence — what this does and who it's for. No technical jargon.]

---

## For the team

### What this is
[2-3 sentences. Technical context. What problem it solves. What makes it notable.]

### Stack

| Layer | Technology | Version |
|-------|-----------|---------|
| Runtime | [from CODEBASE.md] | [version] |
| Framework | [from CODEBASE.md] | [version] |
| Database | [from CODEBASE.md] | [version] |
| Auth | [from CODEBASE.md] | [version] |
| Styling | [from CODEBASE.md] | [version] |
| Testing | [from CODEBASE.md] | [version] |
| Deployment | [from CODEBASE.md / docker-compose / config] | — |

### Getting started

**Prerequisites:** [Node version / Python version / Flutter version — from .nvmrc / pyproject / pubspec]

```bash
# Clone and install
git clone [repo-url]
cd [project-name]
[install command from CODEBASE.md section 2]

# Environment setup
cp .env.example .env
# Edit .env — see .env.example for required variables

# Database setup (if applicable)
[migration command from CODEBASE.md section 2]

# Run development server
[dev command from CODEBASE.md section 2]
```

Open [http://localhost:PORT](http://localhost:PORT)

### Project structure

```
[project-root]/
[Folder map from CODEBASE.md section 3 — key folders only, max 12 lines]
```

Full detail: `vibe/CODEBASE.md`

### Running tests

```bash
[test command from CODEBASE.md section 2]
[e2e command if present]
[coverage command if present]
```

### Architecture

[2-3 sentences summarising the architectural approach from ARCHITECTURE.md overview.]
[How the layers connect. Key patterns in use.]

Full detail: `vibe/ARCHITECTURE.md`

### Key decisions

[Top 3-5 tech choices from DECISIONS.md — the ones most likely to affect new contributors]

- **[Decision]:** [Why it was chosen over alternatives] — `D-[ID]`
- **[Decision]:** [Why it was chosen over alternatives] — `D-[ID]`

Full history: `vibe/DECISIONS.md`

### Code conventions

[Key rules from ARCHITECTURE.md — the 4-5 most important for a new contributor]

- [Convention 1 — e.g. "All API calls go through `src/services/` — never direct in components"]
- [Convention 2]
- [Convention 3]

Commit format: `feat(scope)`, `fix(scope)`, `docs(scope)` — conventional commits.

---

## For the client

### What was built

[Plain English description of the project — 2-3 paragraphs.]
[What problem does it solve? What can users do with it?]
[No technical terms. Written for a business owner, not a developer.]

### Features

[One section per major feature from SPEC.md — plain English only]

#### [Feature name]
[2-3 sentences. What the user can do. What happens when they do it.]

#### [Feature name]
[2-3 sentences.]

### How to access

**Live URL:** [url — or "provided separately"]
**Login:** [instructions — or "credentials provided separately"]
**Support:** [who to contact for access issues]

### Reporting issues

To report a bug or request a change, contact:
[Contact name / email / channel]

Please include:
- What you were trying to do
- What happened instead
- A screenshot if relevant

---

*Last updated: [date]*
*Documentation generated by vibe-document*
*Technical detail: `vibe/SPEC.md` · `vibe/ARCHITECTURE.md` · `vibe/DECISIONS.md`*
```
